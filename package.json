{
  "//": [
    "A1 bootstrap: one pinned dependency graph for every workspace (charter: root deps are a coordinated shared change, never two concurrent lockfile updates).",
    "effect 4.0.0-rc.112 = the `rc` dist-tag; Effect 4 RC is the selected major, Effect 3 is not an acceptable substitute.",
    "@tanstack/ai is the published TanStack AI core package; @tanstack/ai-core does not exist on npm.",
    "@opentelemetry/api is the required peer of @tanstack/ai.",
    "typescript 5.9.3 = last 5.x stable line; the 6/7 native line is a later coordinated decision, not a bootstrap substitution."
  ],
  "name": "kiero",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": ">=22.12.0 <23"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "build": "npm run build --workspace @kiero/web",
    "dev": "npm run dev --workspace @kiero/web"
  },
  "dependencies": {
    "@opentelemetry/api": "1.9.1",
    "@tanstack/ai": "0.53.0",
    "@tanstack/ai-openrouter": "0.19.8",
    "@tanstack/react-query": "5.102.8",
    "@tanstack/react-router": "1.170.33",
    "convex": "1.45.0",
    "effect": "4.0.0-rc.112",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260908.1",
    "@types/node": "22.20.1",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.7",
    "typescript": "5.9.3",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  }
}
