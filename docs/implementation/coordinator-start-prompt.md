# Coordinator starter prompt

Use this prompt for the remaining core implementation map. It grants the coordinator the Git lifecycle for the assigned map; implementation workers own only their bounded issue slice.

---

Implement the remaining accepted Kiero core tracked by https://github.com/wojtekpiskorz/kiero/issues/15. Read AGENTS.md, CONTEXT.md, docs/implementation/README.md and execution-charter.md. Preserve the accepted barebones Polish PWA scope. The 2026-09-12 application review is baseline evidence, not current readiness.

1. Audit live main, local dirty/untracked work, active worktrees, open PRs, issue claims, all native sub-issues and their blocked_by relations. Run `rtk proxy node docs/implementation/audit-map.mjs --remote`. Investigate drift before selecting work. Never reuse another issue's branch, worktree, cloud ref or evidence namespace without resolving ownership. Keep unrelated changes intact.
2. Read the selected issue's complete current body, linked contracts, proof cases and latest comments. An issue is ready only when it is open, unassigned, has no open native blocker, its required resources are available and its owned paths do not conflict with active work. After M0, the expected initial roots are R1, R4, R6 and R7; verify rather than assume them.
3. Claim one issue per implementation assignment. Create an issue-specific `codex/` branch and worktree from current main. Give each worker exact scope, paths, interfaces, required evidence and completion criteria. Use independent ready lanes concurrently; the coordinator retains commits, pushes, PRs, reviews, merges and closure. Keep shared contracts, lockfiles, generated clients and composition changes under one coordinating writer.
4. Before any live proof, reserve its exact Convex ref, Workers, tenant/fixture IDs, bucket namespaces, OAuth accounts, notification destination and device window. Follow the owner constraint at issue15 comment5618841487: refs remain under existing `kiero-dev-core`; never create projects to escape quota. I8 must prove staging isolation and I6 must prove quarantine isolation. If those requirements cannot coexist, keep the task OPEN/BLOCKED and record the exact owner decision required before mutation. Credentials enter runtime secret stores; record names and observed behavior only.
5. Implement the complete issue through its real module boundary. Repair tasks include a regression for the observed failure and the stated integration. Proof tasks use actual services/devices when assigned. Fixtures prove deterministic logic, not actual provider or device availability. If a missing prerequisite is outside ownership, create one bounded issue, cycle-check the proposed graph, then add it as a native blocker and mirror it in the inventory before continuing affected work.
6. Prepare one reviewable PR per issue. Run the focused checks appropriate to the change and the repository's required deterministic checks on the actual head. Inspect the advisory AI review and resolve actionable findings; an unavailable review is an observed failure, not a zero-finding verdict. Compare current main, integrate serially and rerun affected tests after overlap/conflict resolution. Regenerate derived clients when the integrated schema/functions require it; never hand-edit them or claim regeneration without doing it.
7. Attach each required case's candidate, environment/device, package/provider/prompt/schema versions, independent fixture expectation, actual result, reproducible command/procedure and redacted artifact. Preserve failed attempts and cost observations. Mandatory FAIL/BLOCKED/NOT RUN means the issue stays open. If code must merge before external proof can run, keep its issue open and use `Refs #N`, not an automatic closing keyword. Close only after integration and all assigned evidence satisfy acceptance. J3 retains its exact >=48/50 per-run, zero-critical-error threshold.
8. After each merge or closure, refresh native blockers and fill free capacity with independent ready work. Synchronize docs/implementation/issues.json, inventory, graph and proof ownership with the live state in the implementation PR only if that issue explicitly owns those central documents; otherwise use a bounded map-administration issue. Re-run the map audit; a comment alone does not repair a stale native edge. Remove only completed owned worktrees after preserving their result.
9. J6 establishes the repaired real candidate. J3 and J4 then qualify it. Start J4 promptly when ready: two real Google accounts must remain observed for more than seven elapsed days, alongside physical phone/browser/PWA, push and actual release-during-capture cases. Do not accelerate that clock. Track relevant code/configuration changes; rerun affected evidence and explicitly justify reuse of unaffected results.
10. Continue through I6, J3, J4, I9 and I11 to J5. J5 must trace every accepted core function and all 61 UX inventory entries to current operations, reachable barebones controls or operator procedures, owners and P01-P12 evidence. Reproduce the completed core from clean instructions. Missing behavior becomes a concrete prerequisite, never an omitted row or weaker threshold. Core closure hands off to UX integration and alpha preparation; production activation, final design and four-week live-alpha success remain separate.

Before ending a session, leave a durable checkpoint on #15 with current main/PR/issue state, evidence paths, active resource reservations, ready frontier, failed or blocked cases, exact next action and human/time prerequisites. State whether required work remains. A session pause or exhausted capacity is not completion of the map.
