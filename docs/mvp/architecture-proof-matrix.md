# Architecture integration proofs

Prepared on 2026-09-08 for [Grilling: architektura, stack i hosting MVP](https://github.com/wojtekpiskorz/kiero/issues/11). This consolidates evidence already required by the accepted contracts. It is a planning artifact, not a completed verification report or an implementation ticket list. Q209–Q210 service/runtime choices are accepted. Q211's revised Free-first plan and narrower native-platform-log coverage are accepted.

Every row is **NOT RUN**. The repository currently contains planning documents, with no installed application or lockfile. Upstream documentation establishes candidate capability only. A failed proof must produce a concrete repair or a disclosed contract change; it cannot silently weaken an accepted behavior.

## Evidence and sequencing

Record the exact code revision, published package versions, provider route, environment, input fixtures, observed outcome and cost for each proof. Keep independent expected results and a repeatable command or device procedure. Use synthetic or deliberately approved samples. Record temporary service failures separately from incorrect application behavior.

Prove the typed integration, authentication and durable source-publication boundaries first. Media/AI, external delivery and recovery probes can then exercise those foundations. Release and device probes must use the integrated candidate. This dependency order does not create multiple active implementation tickets.

| ID | Boundary | Evidence required before declaring it ready |
| --- | --- | --- |
| P01 | Effect 4 RC, Convex and TanStack | Pin published compatible packages. Compile the shared operation contracts and test invalid external inputs through Effect Schema, Convex validators/codegen and the actual TanStack tool/schema adapter. Prove the standard-schema/JSON-schema conversion used by that package set. No unchecked cast or `any` can stand in for validation at a boundary. Validate client subscriptions through the chosen Convex Query adapter. |
| P02 | Identity, tenant access and GM | Exercise Google and OTP login, both directions of same-email sign-in, explicit linking with both proofs, concurrent linking, targeted invitation acceptance/revocation and last-admin transfer. Prove 30-day inactivity behavior and immediate revocation against existing tokens, subscriptions, file requests, exports, notifications and jobs. GM access is explicit and audited and never changes ordinary read/activity metrics. Calendar disconnection must leave login usable. |
| P03 | Durable source acceptance | Interrupt before/after each upload and publication boundary, including R2 completion followed by failed Convex acceptance. Retry the same logical source and preserve `sentAt`. All attachments must be durable before the saved receipt; source creation and durable workflow registration cannot leave an accepted unprocessed orphan. Reconcile abandoned uploads safely against delayed retries and garbage collection. |
| P04 | Mobile drafts and long recordings | Use actual iOS and Android browsers/PWA installations. Record, play, discard and send; interrupt the connection, close/reopen, exhaust local storage and revoke access. Preserve recoverable drafts and upload progress with an honest saved/unsent state. Test long recordings across decoder/segment boundaries without imposing a product duration cap. Browser chunks, upload parts and decodable STT segments must remain distinct. |
| P05 | Media representations and access | Test received phone formats, rotation, handwriting, small amounts/dimensions and oversized/unsupported images. Verify durable normalized output before input cleanup; exercise the retained-original exception. Vision coordinates and exports must resolve to the actual retained version. Test private streamed/range reads, current access checks and revocation during an active stream. Bound decoder/encoder memory, disk and runtime. Use the Q210 accepted Images/Container executors and scoped job permissions. |
| P06 | Durable analysis and memory publication | Crash/retry after every stage and during external tool execution. Parallel plans must recheck relevant revisions and permissions at commit, preserve newer corrections and publish current fields, revisions and provenance atomically. Exercise ambiguous contradictions, explicit supersession, withdrawal, permanent deletion and independent corroboration. Derived stale data cannot trigger an action as though current. Duplicate provider requests must not duplicate domain effects. |
| P07 | AI, STT and retrieval quality | Run the accepted approximately 50 Polish cases with independent starting state and expected facts, clarifications and prohibited changes. Report text, image, STT and end-to-end outcomes separately. Include dates, relative dates, project aliases, money and unknown tax basis. Exercise the selected chat order, image-capability restriction, stalled/failed routes and actual tool/schema forwarding. Validate MAI/Whisper fidelity and original-audio timestamps, missing segments and backup behavior. Prove Qwen query/document preparation, dimensions, Polish recall, tenant filtering, revision hydration, deletion and index-generation cutover. |
| P08 | Notifications and Google Calendar | Test source-level read/batching/mute/quiet-hour rules, revoked recipients, reminder conflicts and snooze on real devices. Use two real Google accounts to test accepted event mapping, DST, individual deletion/move, reconnect, 404 ambiguity, lost change signals and unknown write outcomes. Prove convergence without duplicates and authorization beyond the OAuth Testing expiry period. Measure the separately defined source-processing and Google-sync targets. |
| P09 | Firm exports | Export a consistent firm snapshot while users keep writing. Compare HTML, versioned JSON, history and retained media to that snapshot; exclude other tenants and auth secrets. Escape untrusted HTML and archive paths. Prove current admin checks, 24-hour availability from completion, revocation and immediate invalidation after permanent source deletion, followed by timely byte cleanup. Q210 selects the bounded Container assembly runtime. |
| P10 | Complete backups and recovery | Run the selected 15-minute EU exporter on the actual service plan, prove export charges/native snapshot-slot cleanup, interrupt/overlap attempts, and verify database snapshot plus every referenced retained media object before marking completion. Measure snapshot age and restore elapsed time against the one-hour RPO and eight-hour RTO. Test 48-hour frequent/14-day daily retention and reference-aware collection. Restore in quarantine; apply an external content-free deletion/revocation ledger before access opens. Prove ledger ordering under failure and recovery of auth/component/workflow state, scheduler configuration and unknown Google/push outcomes. No old backup may resurrect revoked access or deleted content. |
| P11 | Releases and safe PWA updates | Deploy a schema expansion, resume an interrupted migration, update the client and later remove old support. Exercise in-flight older workflow executors, old tabs, recording/upload during update and local-draft schema changes. Prove safe Polish update prompts, compatible rollback/forward repair and environment-scoped secrets. Restoring an old database is not normal release rollback. |
| P12 | Diagnostics, alerts and alpha costs | Inspect default logs, HTTP errors, SDK diagnostics and telemetry for source bodies, audio/image payloads, tokens and secrets. Verify accepted retention, query access, best-effort stream loss and independent detection of missing health signals. Trigger actual owner alerts for source-save failures, failed/stuck processing, stale complete backups and 400/500 PLN thresholds. Account for free-plan limits and metered compute, media, backup, egress, observability and AI costs. Q211 Free-first plan and native-platform-log deferral are accepted; test explicit application-event coverage and provider hard-cap detection without assuming full native log history. |

## Handoff boundaries

The alpha assessment remains four weeks of real use by the two bosses, including the accepted source-processing latency, weekly question accuracy and usage criteria. Synthetic proofs do not satisfy those live outcomes. GM-assisted fixes must remain distinguishable from autonomous performance.

Visual composition and component choices belong to [Prototype: szybki wpis, rozmowa i podsumowanie projektu](https://github.com/wojtekpiskorz/kiero/issues/12). Concrete acceptance and implementation sequencing belong to [Grilling: kryteria gotowości i przekazanie MVP do implementacji](https://github.com/wojtekpiskorz/kiero/issues/13). Encoder profiles, package pins and timeout values should come from these proofs, not unsupported planning constants.
